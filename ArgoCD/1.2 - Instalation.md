# Instalación

En la pagina oficial vamos a encontrar 4 tipos de instalaciones:
```
https://argo-cd.readthedocs.io/en/stable/operator-manual/installation/
```
- Non-High Availability: Recomendado para pruebas
  - **install.yaml:** Instalación estándar de Argo CD con acceso de administrador de clúster.
  - **namespace-install.yaml:** Instalación de Argo CD que solo requiere privilegios a nivel de namespace (no necesita roles de clúster). Recomendado si necesitas diferentes instancias de ArgoCD para diferentes equipos. Este aun te permite desplegar aplicaciones en el mismo cluster o cluster externo, pero necesitas agregar credenciales (i.e. argocd cluster add <CONTEXT> --in-cluster --namespace <YOUR NAMESPACE>)

- High Availability: Recomendado para produccion
  - **ha/install.yaml:** Igual que install.yaml pero con múltiples réplicas para los componentes compatibles.
  - **ha/namespace-install.yaml:** Igual que namespace-install.yaml pero con múltiples réplicas para los componentes compatibles.

## Conexion desde afuera del cluster

Para nuestras pruebas usaremos la siguiente configuracion:

1. Los CRD de Argo CD no están incluidos en namespace-install.yaml y deben instalarse por separado.

```
kubectl apply --server-side --force-conflicts -k https://github.com/argoproj/argo-cd/manifests/crds\?ref\=stable
```

2. Lanzamos la instalacion sin alta disponibilidad

```
kubectl create namespace argocd
```

```
kubectl apply --server-side -f https://raw.githubusercontent.com/argoproj/argo-cd/refs/tags/stable/manifests/install.yaml -n argocd
```

3. Cambiamos la configuracion de ArgoCD server para servir trafico por el puerto 80

```
kubectl patch deployment argocd-server -n argocd \
  --type='json' \
  -p='[{"op":"add","path":"/spec/template/spec/containers/0/args/-","value":"--insecure"}]'
```

4. Reiniciamos el  deployment de argocd-server
```
kubectl rollout restart deployment argocd-server -n argocd
```

5. Hacemos port fordwar para acceder a argocd desde la web

```
kubectl port-forward svc/argocd-server \
-n argocd \
--address 0.0.0.0 \
8080:80
```

6. El usuario por defecto es admin y para obtener el password lanzamos el siguiente comando:

```
kubectl get secret argocd-initial-admin-secret \
-n argocd \
-o jsonpath="{.data.password}" \
| base64 -d
```
## ArgoCD Web UI

Dependiendo de donde estes corriendo kubectl vas a poder acceder al Web UI de ArgoCD, pero en la mayoria de los casos debe ser

```
localhost:8080
```

## ArgoCD cli

La CLI de Argo CD (Interfaz de Línea de Comandos) es una herramienta de terminal que permite interactuar directamente con tu servidor de Argo CD. Es utilizada por desarrolladores y operadores de DevOps para gestionar clústeres de Kubernetes, automatizar despliegues, configurar el sistema y sincronizar aplicaciones sin necesidad de utilizar la interfaz gráfica web. 

Vamos a ver la instalacion para diferentes sistemas operativos

```
https://argo-cd.readthedocs.io/en/stable/cli_installation/
```

1. Instalacion

```
curl -Lo argocd \
https://github.com/argoproj/argo-cd/releases/download/v3.3.0/argocd-linux-amd64

chmod +x argocd
sudo mv argocd /usr/local/bin/
```

2. Removemos la instalacion --insecure

```
kubectl patch deployment argocd-server \
-n argocd \
--type='json' \
-p='[{"op":"remove","path":"/spec/template/spec/containers/0/args/1"}]'
```

3. Reiniciamos el  deployment de argocd-server

```
kubectl rollout restart deployment argocd-server -n argocd
```

4. Hacemos port fordwar para acceder a argocd desde el cli

```
kubectl port-forward svc/argocd-server \
-n argocd \
--address 0.0.0.0 \
8080:443
```
5. Login en el cli

```
argocd login localhost:8080
```

## Conexion desde dentro del cluster

1. Vamos a buscar el pod que esta corriendo el argocd-server

```
kubectl get pods -n argocd | grep 'argocd-server'
```
Vas a obtener algo similar a
```
argocd-server-5c4774867c-w9qvv                     1/1     Running   0          14m
```

2. Accedemos al shell del pod
```
kubectl exec -it argocd-server-5c4774867c-w9qvv -n argocd -- bash
```
 
3. Lanzamos el login
```
argocd login localhost:8080
```




